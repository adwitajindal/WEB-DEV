#include <stdio.h>
int main()
{
    float num1, num2;
    printf("Enter the value of first number: ");
    scanf("%f", &num1);
    printf("Enter the value of second number: ");
    scanf("%f", &num2);
    printf("the sum of both numbers is %.2f \n", num1 + num2);
    printf("the difference between both numbers is %.2f \n", num1 - num2);
    printf("the product of both numbers is %.2f \n", num1 * num2);
    printf("the division of a and b gives %.2f \n", num1 / num2);

}


